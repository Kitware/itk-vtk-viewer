#!/usr/bin/env node
const path=require("path"),fs=require("fs-extra");mainPackageFile=fs.readFileSync(path.join("package.json")),mainPackage=JSON.parse(mainPackageFile),version=mainPackage.version;const packages=["image-io","mesh-io"];packages.forEach((e=>{const a=fs.readFileSync(path.join(__dirname,`${e}.package.json.in`)),i=JSON.parse(a);i.version=version,i.dependencies["itk-wasm"]=version,fs.writeFileSync(path.join("dist",e,"package.json"),JSON.stringify(i)),readmeFile=path.join(__dirname,`README.${e}.md`),fs.copyFileSync(readmeFile,path.join("dist",e,"README.md"))}));