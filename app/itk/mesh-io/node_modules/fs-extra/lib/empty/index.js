"use strict";const u=require("universalify").fromPromise,fs=require("../fs"),path=require("path"),mkdir=require("../mkdirs"),remove=require("../remove"),emptyDir=u((async function(r){let e;try{e=await fs.readdir(r)}catch{return mkdir.mkdirs(r)}return Promise.all(e.map((e=>remove.remove(path.join(r,e)))))}));function emptyDirSync(r){let e;try{e=fs.readdirSync(r)}catch{return mkdir.mkdirsSync(r)}e.forEach((e=>{e=path.join(r,e),remove.removeSync(e)}))}module.exports={emptyDirSync,emptydirSync:emptyDirSync,emptyDir,emptydir:emptyDir};